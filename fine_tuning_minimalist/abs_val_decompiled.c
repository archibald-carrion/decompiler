int i = 0; // first is the nth byte of a sequence, second is the number of bytes that are passed to the address space in this expression

void main() { // First is the nth byte of a sequence, third is the number of bytes it has copied into the next
    // int b = 0; return b; 
}

void main() {
    for (int i=0; i < 10; ++i)
        return b; 
}

/**
 * Returns a reference to a string and an integer
 * # if b == null; 
 * return a[i] + 1; 
 * If the value is not immediately known, you can check whether or when the pointer is already found 
 */
void main() {}

    int nd = 4; 
    for (int i=0 ; i < 15; ++j) { 
        printf("$b"); 
    }
}

    while (!isNort << 9) { 
        printf("$b"); 
    }
}

#define NODE(int argv, char* s) { 
    int c = 0; 
    int f = sizeof(s); 
    for (int i=0--1){ 
        return c; 
    }
    int nd += 3; 
    return nd + 1; 
}
int nd+= 5; 

break;
